import 'package:flutter/material.dart';

abstract class Styles {
  static const BigTitle = TextStyle(fontSize: 24, fontWeight: FontWeight.bold);
  static const MediumTitle = TextStyle(fontSize: 18);
}
